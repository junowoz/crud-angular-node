{
  "name": "digiboard-back",
  "version": "1.0.0",
  "description": "3. Run `prisma db pull` to turn your database schema into a Prisma schema.\r 4. Run `prisma generate` to generate the Prisma Client. You can then start querying your database.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.5.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.8.8"
  }
}
